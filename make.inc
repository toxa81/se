# root directory of software environments
export SE_ROOT=/apps/hohgant/se

# location of the spack installation
SPACK=$(SE_ROOT)/spack/bin/spack

export SPACK_SYSTEM_CONFIG_PATH=$(SE_ROOT)/spack-config
